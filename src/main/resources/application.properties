#mysql dataBase config
spring.datasource.url=jdbc:mysql://10.0.25.88:3306/chic_car_outer?useSSL=false
spring.datasource.username=chic_car_outer
spring.datasource.password=China@123
spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.datasource.max-idle=20
spring.datasource.max-wait=30000
spring.datasource.remove-abandoned=60000
spring.datasource.min-idle=2
spring.datasource.initial-size=10

# Specify the DBMS
spring.jpa.database = MYSQL
# Show or not log for each sql query
spring.jpa.show-sql = true
# Hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto = update
# Naming strategy
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy
# stripped before adding them to the entity manager)
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

spring.jpa.properties.hibernate.current_session_context_class = org.springframework.orm.hibernate5.SpringSessionContext

#server config
#server.port=8011
#server.session.timeout=10
server.tomcat.uri-encoding=UTF-8

#initSqlPath=D:\\workSpaces\\servicePlatform\\servicePlatform\\target\\mysqlTable.sql
initSqlPath=/home/mysqlTable.sql
